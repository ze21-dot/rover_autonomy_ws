# rover_msgs

> Part of `rover_autonomy_ws` — governed by **SUR-AUT-SPEC-001 Rev 1.0**. README format per **CNV-4**.

Custom messages and services (rosidl), if any.

## Purpose

Custom interfaces only when no standard message fits (prefer common_interfaces). Currently empty by design — do not add messages speculatively.

## Interfaces

None yet.

## How to run the test

`colcon build --packages-select rover_msgs` succeeds; generated headers importable.

## Related spec items

See the traceability table in the root README and Jira for the task IDs this package owns.
